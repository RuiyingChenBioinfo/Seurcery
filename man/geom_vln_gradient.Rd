% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils_geom_vln_gradient.R
\name{geom_vln_gradient}
\alias{geom_vln_gradient}
\title{Add gradient fill polygons to violin plots}
\usage{
geom_vln_gradient(
  bin = 50,
  alpha_max = 1,
  alpha_min = 0,
  direction = 1,
  layer = NULL,
  outline = TRUE,
  outline_size = NULL,
  min_width_frac = 0.02,
  high_quantile = 1,
  mid_quantile = 0.5,
  low_quantile = 0
)
}
\arguments{
\item{bin}{Number of vertical bins used to reconstruct the gradient fill.
Must be a single number greater than or equal to 2.}

\item{alpha_max}{Maximum alpha value.}

\item{alpha_min}{Minimum alpha value.}

\item{direction}{Gradient direction. Use \code{1} for low to high alpha from low
y to high y, and \code{-1} for the reverse.}

\item{layer}{Optional index of the violin layer in the ggplot object. If
\code{NULL}, the function tries to detect the first violin-like layer
automatically.}

\item{outline}{Logical. Whether to redraw violin outlines.}

\item{outline_size}{Optional outline width. If \code{NULL}, uses the linewidth
in the source violin layer when available.}

\item{min_width_frac}{Minimum local width threshold, expressed as a fraction
of the maximum violin width, below which thin polygon slices are skipped.}

\item{high_quantile}{Upper anchor point of the alpha interpolation, in
\verb{[0, 1]}.}

\item{mid_quantile}{Middle anchor point of the alpha interpolation, in
\verb{[0, 1]}.}

\item{low_quantile}{Lower anchor point of the alpha interpolation, in
\verb{[0, 1]}.}
}
\value{
An object of class \code{"geom_vln_gradient"} that can be added to a
ggplot object.
}
\description{
Creates a lightweight layer-like object that can be added to a ggplot object
with \code{+}. The object is handled by the S3 method
\code{ggplot_add.geom_vln_gradient()}, which reconstructs violin polygons from an
existing violin layer and fills them with an alpha gradient along the y axis.
}
\details{
This function supports both a single ggplot violin plot and a patchwork
object returned by \code{Seurat::VlnPlot()} when multiple features are combined.
}
